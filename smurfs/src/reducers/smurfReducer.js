
const initialState = {
    chars: [], 
    test: 'Its working'
}

export const reducer = (state = initialState, action) => {

    switch(action.type){

        default:
            return state;
    }
}